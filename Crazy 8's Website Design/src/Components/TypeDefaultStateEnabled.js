import React from 'react'
import './TypeDefaultStateEnabled.css'
export default function TypeDefaultStateEnabled (props) {
	return (
		<div className={`TypeDefaultStateEnabled_TypeDefaultStateEnabled ${props.className}`}>
		</div>
	)
}